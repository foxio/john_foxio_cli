language: go
sudo: false
go:
         - 1.5
         - 1.6
         - tip

install:
  - go get -v github.com/codegangsta/cli
script:
    - make test
    - make build

matrix:
    allow_failures:
        - go: tip
